extends ../templates/base

append config
	-currentPage = 4
	| {% load markdown_deux_tags %}

block header
	section.acm-article-cover
		img(src="https://placehold.it/1200x400")
		h1.title Taller de pensamiento algoritmico


block maincontent
	section.markdown-body
		| {{ "# **Hola**" | markdown }}
